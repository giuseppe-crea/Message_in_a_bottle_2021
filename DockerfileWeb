FROM python:3.9-alpine
WORKDIR /code
ADD . /code
ENV DOCKER=yes
ENV TZ=Europe/Rome
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone
RUN apk add build-base
RUN pip install -r requirements.txt
RUN chmod +x ./run_docker.sh
CMD ["/bin/sh", "-c", "./run_docker.sh"]
